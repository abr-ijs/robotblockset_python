MJCF model assets for RBS.

This package contains only model data files and installs them into:

- `robotblockset/mujoco/mjcf_models`

Install together with the main package:

```bash
pip install robotblockset[mjcf]
```
